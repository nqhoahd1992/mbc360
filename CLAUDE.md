# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

MBc360 is a Product Development & Quality "Phase-Gate" system, modeled 1:1 on a 55-sheet Excel workbook (`MBc360 Master Product Development System File.xlsx`, version "V18"). Workbook tabs are owner-prefixed (e.g. `ChiChu-Prohibited_Ingred`); each sheet carries a "REVIEW OWNER" header surfaced in the app via `reviewOwner` on phase/register configs. V18 added Phase 3 "Extended User-Safety Compartments" (Pregnancy/Breastfeeding/Infant/Swimmer), the Released Label Control register (3 blocks) and system-reference sheets (Template_Index, Requirements, MBC360 FEEDBACK). See `docs/APP_PLAN.md` (Vietnamese) for the full source-of-truth spec — business rules, data model, screen list — `docs/Business_Rules_Confirmation_{EN,VN}.md` for confirmed business-rule decisions, and `docs/BACKEND_PLAN.md` (Vietnamese) for the backend build plan.

The frontend started as a backend-less demo UI (state in localStorage) and still behaves that way; the production backend is being built per `docs/BACKEND_PLAN.md` (milestone M0 — monorepo + API skeleton + Docker packaging — is done).

## Monorepo layout (npm workspaces)

- `apps/web` — `@mbc360/web`: the React + Vite frontend (all the original demo code).
- `apps/api` — `@mbc360/api`: NestJS backend (skeleton: `/api/health`, `/api/meta/*`).
- `packages/shared` — `@mbc360/shared`: canonical types (`src/types`), workbook config (`src/config`), and the rule engines (`src/utils/gateProgress.ts`, `src/utils/ingredientWatch.ts`). **Both web and api must consume rules from here — never fork a copy.** Imported via subpaths: `@mbc360/shared/types`, `@mbc360/shared/config/gates`, `@mbc360/shared/utils/gateProgress`.

## Commands

All from the repo root:

```
npm run dev       # build shared once, then run shared (tsc -w) + api (:3000) + web (Vite HMR) concurrently
npm run build     # build shared -> api -> web (order matters: apps compile against shared's dist)
npm run lint      # oxlint across the whole repo
docker compose -f docker-compose.dev.yml up -d   # dev Postgres (apps are NOT containerized in dev)
npm run db:migrate -w @mbc360/api   # prisma migrate dev (needs dev Postgres up + apps/api/.env, see .env.example)
npm run db:seed -w @mbc360/api      # idempotent seeder: rule config + demo project scaffolded from shared config
npm run db:setup                    # all three of the above in order — run this after a fresh clone or a wiped/new Postgres volume
```

**Fresh/reset dev Postgres → `npm run db:setup` before `npm run dev`.** A new (or volume-wiped) dev Postgres container starts with an empty `public` schema — no `users` table, no seeded roles. `npm run dev` itself never runs migrations (deliberately — migrations must be a conscious step), so starting the API against a schema-less DB surfaces as `PrismaClientKnownRequestError P2021: The table "public.users" does not exist` the moment anyone hits an endpoint that touches Prisma (e.g. the OIDC callback in `AuthService.upsertSsoUser`). Always run `npm run db:setup` (or the 3 commands above in order) once after: first clone on a new machine, `docker compose down -v` / any Postgres volume reset, or pulling a branch with new Prisma migrations.

Per-workspace: `npm run <script> -w @mbc360/web|@mbc360/api|@mbc360/shared`. If TypeScript can't resolve `@mbc360/shared/*`, build the shared package first (`npm run build -w @mbc360/shared`). The Vite dev server proxies `/api` → `localhost:3000` (same-origin, mirroring production nginx routing) — the frontend must always call relative `/api/...` URLs.

`apps/web/vite.config.ts` force-prebundles every `@mbc360/shared/*` subpath through esbuild (`optimizeDeps.include`), needed so the dev server's native-ESM import handling can read named exports off `shared`'s CJS build — see the comment there; extend that list when a NEW `@mbc360/shared/config/*` or `/utils/*` subpath is imported for the first time, or you'll get a "does not provide an export named ..." error. **Fixed 2026-07-21 (was a recurring gotcha before this):** `optimizeDeps.force: true` makes every dev-server start unconditionally re-bundle from the current `packages/shared/dist` instead of trusting the on-disk prebundle cache (which used to be keyed off the lockfile/config, not that content, so it could survive even a full `npm run dev` restart), and the root `predev` script (`kill-port 5173 3000`) clears any zombie dev process left listening from an improperly-stopped previous session (only `Ctrl+C` in the owning terminal, or `taskkill /T /F` on the root PID, actually tears down the whole `concurrently` → `tsc`/`vite`/`nest` tree — a plain `kill $PID` leaves the children running, which is a second way this used to bite). **Plain `npm run dev` now self-heals both; `npm run dev:clean` still exists but is no longer the primary fix.**

There is no test runner configured. Production packaging: `apps/{api,web}/Dockerfile` (build context = repo root) + `docker-compose.prod.yml`, fronted by the host nginx (`deploy/nginx.host.example.conf`).

## Language rule

All code — variable/function names, comments, strings, error messages, UI labels — must be **100% English**, matching the real workbook's dropdown/column names exactly (see "Config-driven" note below).

## Architecture

### Phase-Gate domain model

The business process is **4 Phases containing 12 Gates (SG01–SG12)**, plus cross-cutting Change Control. Phase/Gate metadata (owners, purpose, key outputs) lives in `packages/shared/src/config/gates.ts` (`PHASES`, `GATES`, `EIGHT_ANGLES`, status/decision enum arrays). Gate progression rules live in `packages/shared/src/utils/gateProgress.ts` and implement the **confirmed business rules** in `docs/Business_Rules_Confirmation_{EN,VN}.md` (rule IDs like B1/B3/C1 below refer to that document — read it before changing any rule):

- A gate is **passed** (B1) only when `status === 'Complete'`, a positive `decision` (`Proceed` / `Proceed with Conditions`) is recorded, AND `gateBlockers()` is empty — open next actions block a plain `Proceed` (allowed to stay open only under `Proceed with Conditions`, B2), and the Skincare-for-Two safety screen hard-blocks SG07 when the Gate 02 target users include Pregnancy/Breastfeeding/Postpartum (C1). A `Gap` status prevents a normal `Proceed` (enforced in both `GateFlowTable` and the store's `setGate`).
- A **phase** is complete (B3) only per `phaseCompletionChecklist`: all gates passed + key gate checks done (or N/A **with justification** in notes) + all 8 angles covered (or justified N/A) + next actions closed (unless Proceed with Conditions) + all three sign-off roles signed. `SignOffBlock` stays locked until `canSignOff` is true.
- Gates unlock **strictly in order** — `isGateUnlocked` / `currentGateIndex` compute the single gate currently open for work.
- `backtrackGate` (B4, "no silent corrections") reopens a gate range and invalidates affected approvals but **never deletes**: pre-backtrack gate records and sign-offs are snapshotted into `ProjectData.backtrackEvents` (shown on Project Overview). `createFormulaVersion` (A2) reuses this for Major formula changes: bumps `formulaVersion`, reopens SG04–SG09, invalidates Phase 2–3 approvals and appends to the Formulation Change Register. It also snapshots the outgoing Formula BOM into the new `FormulaVersionRecord.previousBomSnapshot` (2026-07-17) — `BomCosting.tsx`'s "Formula version history" card uses this (plus the live `bom`) to view/diff any two versions via `packages/shared/src/utils/formulaDiff.ts` + `FormulaVersionCompareModal`; history entries created before this existed have no snapshot and show as "not available" rather than a fabricated diff.
- Gates 10–12 are additionally tracked **per market** in `ProjectData.marketTracks` (A1); a market's launch approval is hard-blocked until its PIF status is Approved (C5) — enforced in `setMarketTracksBulk` and `MarketTrackingCard`.
- The Study/Human Trial approval trail (C2) is a dedicated 3-role workflow (`ProjectData.studyApprovals`, `StudyApprovalCard` on the Study Protocol register); the Independent Reviewer must not share the Study Author's department (enforced in `setStudyApprovalsBulk` and the UI).

When changing gate/phase logic, `gateProgress.ts` is the single place that encodes these rules — pages/components only call its exported helpers (`gateState`, `phaseProgress`, `gateBlockers`, `phaseCompletionChecklist`, etc.), they don't re-derive pass/lock state themselves.

### RBAC simulation & integrations

- **RBAC (A4, demo simulation):** the header's "View as" selector (`viewRole` in the store) stands in for the logged-in user's role. `apps/web/src/utils/roles.ts` derives authorization from each gate's `primaryOwner` / phase `department` by keyword match — gate decisions, the phase "Approved by" row and market-track approvals are restricted; evidence fields stay open to contributors. The real role matrix / SSO source is follow-up **F6** — replace `roles.ts` when confirmed.
- **Integrations (A3):** `/integrations` page + `apps/web/src/integrations/cosmetri.ts`, a thin client that calls MBc360's own backend proxy (`apps/api/src/cosmetri/cosmetri-data.service.ts`) for formulas, raw materials (`supplier_name`, `status_label`) and compliance (INCI/CAS) — real Cosmetri data end-to-end since 2026-07-17, no more frontend mock (see `docs/swagger-init.json` for the underlying Cosmetri shapes). Cosmetri is strictly read-only; the Formula BOM can be imported from it (`CosmetriImportModal`, gated on `useCosmetriStatus().connected`). `/formula/list`+`/formula/details` only return IDs/reference/quality-status (no product title or formula version), so the picker list shows those two blank until a formula is actually selected — a real Cosmetri API limitation, not a bug. Matching a composition row's INCI/CAS is a best-effort join by raw-material trade name against `/compliance/{id}`'s `INCI_source_raw_materials[].raw_title`, since that endpoint carries no raw-material ID. Data the API doesn't provide (supplier details beyond the name, SDS/CoA/TDS links) is entered manually. New raw materials go through the Power Apps "Create new raw material" change request (URL configurable on the Integrations page — currently a placeholder). Keep Cosmetri strictly read-only in `CosmetriDataService` — never call its `PUT /raw-material/update`.
- **Ingredient screening (C3):** `packages/shared/src/utils/ingredientWatch.ts` auto-checks every Formula BOM line against the prohibited / PB-caution watch-lists — by **CAS number first** (exact; from the Cosmetri import), then INCI-name keywords. The per-group CAS mapping is a demo stand-in (follow-up F3).

### Status & pending work (as of 2026-07-21)

**Frontend:** all decisions confirmed by the subject-matter team are implemented (rules A1–A4, B1–B4, C1–C6). **Update (2026-07-21): the team answered all 14 follow-ups F1–F14 plus A5/B5/C7** (`docs/Response.txt`, recorded in `docs/Business_Rules_Confirmation_{EN,VN}.md`; the questions we still owe the team are in `docs/Business_Rules_Followup_Round2.md`). The **unblocked** follow-ups are now implemented on the frontend (each its own commit):

- **F1/C7** — 3-tier gate-readiness engine: `packages/shared/src/config/gateReadiness.ts` (Mandatory/Conditional/Supporting per-gate lists, transcribed from `Response.txt`) + `gateReadiness()` in `gateProgress.ts`; auto-evaluated Mandatory checks feed `gateBlockers()` (prohibited-ingredient screen on SG04/SG07). Most per-gate items are `manual` (never hard-block) until the team maps each to a concrete register (Round-2 §B). The **Gate Readiness panel UI** is intentionally deferred until that mapping arrives (it would otherwise render mostly "pending confirmation").
- **F5** — `MAJOR_CHANGE_CRITERIA` (`config/changeTriggers.ts`) drives the Major/Minor auto-classification + required reviewer-confirm field in `FormulaVersionModal`.
- **F8** — `NextAction` full workflow (6 statuses + Critical priority; Critical blocks even Proceed with Conditions; "Verified by").
- **F9** — `ChangeStatus` lifecycle + `isChangeOpen()`; an open change soft-locks the gate (blocks plain Proceed + acknowledge modal + store guard).
- **F11** — 12-state `PUBLISHED_INFO_STATES` workflow + reviewer-role columns on the `publishedInfoApproval` register (Claims Library **content** still pending, §B).
- **F13** — pre-work on a locked phase (`PhaseClosure.preWork` + `acceptPhasePreWork`); PhasePage notice + owner-accept (phase-level advisory; per-entry tagging is a follow-on).
- **F14** — `BomLine.reconciled` + `bomReconciled` readiness check hard-blocking SG07/SG10/SG11 until manual BOM lines are reconciled to Cosmetri.
- **F7** was effectively already done (a Gap blocks a plain Proceed); the "critical gap → Hold/Backtrack" branch waits on **A1**.

**Still blocked** (need a pending answer or are heavy architecture): **F2** (needs A2 — the Infant & Baby Safety workflow content), **F3** (needs the real CAS watch-list dataset), **F4** (concurrent formula versions — heavy M3/Prisma data-model work, plus A3 for the same-market case), **F6** (real role×gate matrix + AD mapping + e-signature), **F10** (per-market checklist content), **F12** (Cosmetri ASEAN/VN coverage — external). When a rule changes, update BOTH the code and `docs/Business_Rules_Confirmation_{EN,VN}.md` (keep its follow-up table free of code jargon: it is read by the research team). The 3 clarifications these still-blocked items raised, plus the priority order for tackling them once answered, are in `docs/Business_Rules_Followup_Round2.md`.

**Unrelated small fixes (2026-07-21, same day, not rule-driven):** an **admin-only Cosmetri token inspection** surface — `GET /api/integrations/cosmetri/secrets` (admin-gated, every read audited as `cosmetri.secrets_viewed`) plus a masked reveal/copy UI on the Integrations page — for debugging the live OAuth connection without touching the deliberately token-free `/status` endpoint any signed-in user can read. The dev-loop reliability fix (Vite prebundle cache + zombie process cleanup) is covered under Commands above. A `deploy.sh` at the repo root automates the `docs/DEPLOY.md` §5/§9 redeploy steps (pull → build → migrate → health-check) for quick production redeploys.

**Backend:** being built per the milestone roadmap in `docs/BACKEND_PLAN.md` (M0–M6). Done: **M0** (2026-07-16) — monorepo (`apps/web`, `apps/api`, `packages/shared`), NestJS skeleton with `/api/health` + `/api/meta/*`, production Dockerfiles + `docker-compose.{dev,prod}.yml` (self-hosted, host nginx routes `/` and `/api`), CI workflow. **M1** (2026-07-17) — Prisma 7 schema (`apps/api/prisma/schema.prisma`: current-state tables + append-only `audit_events`, `formula_versions` as its own entity, rule-config tables `gate_requirements`/`safety_triggers`/`watchlist_entries`), first migration, `PrismaService` (pg driver adapter; generated client in `apps/api/src/generated/`, gitignored, `moduleFormat = "cjs"`), global `AuditService` (audit row in the same transaction as the mutation), and a seeder (`prisma/seed.ts` → `npm run db:seed -w @mbc360/api`) built on `apps/api/src/projects/project-scaffold.ts` — the server-side port of `store/factory.ts`, to be reused by `POST /projects` in M3. Dropdown-valued columns are plain strings holding exact workbook values (validated against `@mbc360/shared` lists at the API layer — no DB enums). **M2** (2026-07-17) — auth & RBAC: Entra ID OIDC (code + PKCE via `openid-client`; session = JWT in an httpOnly cookie; `apps/api/src/auth/`), user + department synced from Microsoft Graph at login, **dev mode** while the Entra app registration is pending (`POST /api/auth/dev-login` with a seeded `…@demo.mbc360.local` user; auto-disabled in production), global `SessionAuthGuard` (routes need `@Public()` to opt out), and `PermissionsService` (`apps/api/src/rbac/`) for M3 endpoints. The demo role model moved to `packages/shared/src/config/roles.ts` (single source; `apps/web/src/utils/roles.ts` is now a re-export) and is materialized by the seeder into `roles`/`permissions`/`role_permissions` — replace seed data only when F6 answers. Real Entra app registration is done and SSO was smoke-tested end-to-end (login → Graph-synced department → audit event). **Sign-out completed** (2026-07-17): `POST /api/auth/logout` used to only clear the local session cookie, leaving the user's Entra SSO session alive (a subsequent "Sign in with Microsoft" would silently re-authenticate with no prompt); it now also performs RP-Initiated Logout — the session JWT carries the Entra `id_token` (`AuthService.issueSession`, never used for API calls, only as `id_token_hint`), and `AuthService.logout()` builds the Entra `end_session_endpoint` URL (`openid-client`'s `buildEndSessionUrl`) which `useSession().logout()` navigates the browser to; falls back to a local-only logout (just `appBaseUrl`) for dev-login sessions or if OIDC isn't configured. **Deployment decision confirmed by the user (2026-07-17): self-hosted only, no Vercel or other external frontend host** — there never was any Vercel config in this repo, so `docker-compose.prod.yml` + the host nginx (see M0) is and remains the one deployment path; fixed a real gap found while wiring sign-out: the `api` service's `environment:` block only listed `NODE_ENV`/`PORT`/`DATABASE_URL`, so `SESSION_SECRET`/`AUTH_TENANT_ID`/`AUTH_CLIENT_ID`/`AUTH_CLIENT_SECRET`/`APP_BASE_URL`/`AUTH_REDIRECT_URI` were never reaching the container even if set in the untracked prod `.env` (Compose only auto-injects that file for `${VAR}` substitution, not into a service's environment) — production would have crashed on missing `SESSION_SECRET` or silently run with SSO disabled. Now wired through; `APP_BASE_URL`/`AUTH_REDIRECT_URI` still need to be set to the real domain (and that redirect URI registered on the Entra app) before going live. **Real deployment target identified (2026-07-17): a shared VPS already hosting several other mbcstaging.com projects behind one nginx** (`sites-available/mbc` on that box) — its default ports 8080 (web) / 3000 (api) collide with an already-running project there (`products.mbcstaging.com`), so `docker-compose.prod.yml`'s web/api host-port mappings are now overridable via `WEB_HOST_PORT`/`API_HOST_PORT` env vars (default unchanged: 8080/3000) instead of hardcoded. `deploy/nginx.host.example.conf` stays the generic single-tenant example; `deploy/nginx.mbcstaging.conf` is the real block for this VPS (ports 8086/3004 — the whole 3000–3003 range turned out to be occupied there, discovered during the first real deploy on 2026-07-17; matches that server's existing per-project style — log paths, Certbot cert layout, http->https redirect idiom). **First real deploy performed 2026-07-17** (repo cloned to `~/mbc360_app` on the VPS, appended to the shared `sites-available/mbc`): the full walkthrough — including migrate/seed via one-off node containers on the compose network (migrations do NOT run at container start, and prod Postgres exposes no host port) and every pitfall actually hit (Compose eating `$` in `.env` values, Entra Secret ID vs secret Value → AADSTS7000215, docker group membership, `sudo nginx -t`, DNS-before-certbot) — is written up in `docs/DEPLOY.md`, with `.env.example` at the repo root as the template for the untracked prod `.env`. `server_name mbc360.mbcstaging.com` is the subdomain used for that deploy; its DNS A record still needed pointing at the VPS as of the certbot attempt. New SSO users start with no role until an admin assigns one — that assignment now has a real surface: `apps/api/src/admin/admin-users.controller.ts` (`GET/PUT /api/admin/users`, `GET /api/admin/roles`, admin-only, single role per user, every change audited) plus a web page at `/admin/users` (`apps/web/src/pages/AdminUsers.tsx`, only linked in the sidebar when the real session holds the admin role — `apps/web/src/auth/useSession.ts`). The role is a decision made inside MBc360, never inferred from Graph/AD data. **TEMPORARY dev-phase override (2026-07-17, requested by the user — remove once real role assignment is wanted):** `AuthConfig.autoAdminRole` (`auth-config.ts`, env `AUTH_AUTO_ADMIN_ROLE`, **off by default, opt-in with `="true"`, purely env-controlled in every environment including production — no automatic production lockout, by explicit user request**) makes `upsertSsoUser` (`auth.service.ts`) grant the admin role automatically to any real Microsoft 365 sign-in that currently has zero roles — solves the chicken-and-egg "no admin exists yet to assign the first one" problem while testers need full access. Only ever fires for a user with NO existing role, so it never overrides a role an admin deliberately assigned; each grant is audited as `user.auto_admin_dev`. ⚠️ Unlike `devMode`, there is nothing stopping this from running in production if the env var is left `true` there — unset it (or set anything other than `"true"`) before any deployment with real company users who shouldn't all become admin. `AuthStatus` in the header shows the real signed-in identity alongside (not instead of) the "View as" simulator, which still drives all gate/phase permission checks in the UI until M3 rewires the frontend to the API. **The app is now sign-in-gated** (2026-07-17, superseding the earlier "unauthenticated visitor can still use the demo" note): `App.tsx`'s `Shell` renders a full-page spinner while `useSession()` resolves, then `apps/web/src/pages/Login.tsx` (Microsoft 365 sign-in only — no dev-login UI, that stays a raw endpoint for local testing) if there's no session at all, whether never signed in, signed out, or the session expired — nothing else in the app mounts until a session exists. `AuthStatus` no longer has a signed-out state to render (simplified to assume `user` is always present). **Cosmetri token management pulled forward from M4** (2026-07-17, ahead of M3): `apps/api/src/cosmetri/` — `cosmetri_connection` (singleton row) holds a server-side access/refresh token pair; an admin bootstraps it once via `POST /api/integrations/cosmetri/connect` by pasting a token pair obtained out-of-band (Cosmetri has no OAuth2 client/app-registration flow, just direct username/password/token issuance), which the backend validates immediately by exchanging the refresh token (also yields a freshly-rotated pair, so no manual expiry-date entry); `CosmetriRefreshJob` (`@nestjs/schedule`, cron every 10 min, 15-min buffer) keeps the access token alive well before its 1-hour expiry so the real Cosmetri password is never needed again in normal operation; `POST /api/integrations/cosmetri/refresh-now` is the manual fallback (admin) for when the job/API was down past expiry. `GET /api/integrations/cosmetri/status` is readable by any signed-in user (no tokens in the response — status only), connect/disconnect/refresh-now are admin-only. The web Integrations page (`apps/web/src/pages/IntegrationsPage.tsx`) now drives entirely off this backend state (`useCosmetriStatus` hook) instead of the old frontend-only mock login; `BomCosting.tsx`'s "Import from Cosmetri" gate reads the same hook. **Verified against the real Cosmetri account through the UI.** **BOM-import data endpoints wired to real Cosmetri** (2026-07-17, ahead of M3): `CosmetriDataService` (`apps/api/src/cosmetri/cosmetri-data.service.ts`) proxies `/formula/list`, `/formula/details`, `/formula/{id}`, `/raw-material/details` and `/compliance/{id}` using the stored access token, exposed as `GET /api/integrations/cosmetri/formulas` and `GET /api/integrations/cosmetri/formulas/:id/import-rows`; `apps/web/src/integrations/cosmetri.ts` now calls those instead of returning mock rows — no frontend Cosmetri mock left. Still missing (M4 remainder): a response cache/polling layer (every import hits Cosmetri live; fine at demo volume) and raw-material data outside the Formula BOM import flow (e.g. a standalone raw-material lookup page). **Next: M3** — core API + rule engine server-side + switch the frontend from localStorage to the API. Then: rest of M4 (Cosmetri response caching), M5 advanced workflows, M6 ops/UAT. None of F1–F14 blocks starting; only F4 (needed before M5) and the e-signature part of F6 (before M6) are schedule-relevant.

Where each follow-up answer lands (status as of 2026-07-21 — ✅ implemented, ⏳ blocked):

| Follow-up | Status | Wire the answer into |
|---|---|---|
| F1 — per-gate mandatory sign-offs/evidence (blocks B1/C7) | ✅ engine done; panel UI + register mapping pending §B | `gateReadiness.ts` config + `gateReadiness()`/`gateBlockers()` in `gateProgress.ts` |
| F2 — "Infant 0+" gets its own Infant & Baby Safety workflow (NOT Skincare for Two) | ⏳ needs A2 (workflow content) | `SKINCARE_FOR_TWO_TRIGGERS` in `gateProgress.ts` (trigger already correct) + a new infant-safety workflow |
| F3 — real CAS mapping + market restriction lists | ⏳ needs CAS dataset (§B) | replace the demo tables in `packages/shared/src/utils/ingredientWatch.ts` |
| F4 — Major version × launched markets, concurrent versions | ⏳ heavy M3/Prisma; A3 for same-market | `createFormulaVersion` in the store + the `marketTracks` model |
| F5 — Major vs Minor criteria | ✅ done | `MAJOR_CHANGE_CRITERIA` + auto-classification in `FormulaVersionModal` |
| F6 — real role matrix / SSO / e-signature | ⏳ needs role×gate grid + AD mapping (§B) | replace `apps/web/src/utils/roles.ts` (the whole "View as" simulation) |
| F7 — Gap blocks plain Proceed | ✅ base done; "critical → Hold/Backtrack" needs A1 | decision-option disabling in `GateFlowTable` + the `setGate` guard |
| F8 — Next Action workflow + who closes; priority list | ✅ done (verify/close authorisation stub → F6) | `NextActionsCard` + `NextAction` types |
| F9 — change soft-lock semantics ("open" statuses, acknowledgement) | ✅ done | `ChangeStatus`/`isChangeOpen` + `openChangesForGate` + `setGate` guard |
| F10 — non-ASEAN market checklists (EU CPSR, AU, US) | ⏳ needs checklist content (§B) | new `RegisterConfig` entries in `packages/shared/src/config/registers.ts` |
| F11 — published-info workflow states/roles per content type | ✅ workflow done; Claims Library content pending §B | `PUBLISHED_INFO_STATES` + `publishedInfoApproval` config + violation check in `RegisterHubPage` |
| F12(c) — Cosmetri compliance coverage of ASEAN/VN | ⏳ external (Cosmetri) | informational (affects C5 checklist choice per market) |
| F13 — pre-work on a locked phase + owner acceptance | ✅ phase-level done; per-entry tagging is a follow-on | `PhaseClosure.preWork` + `acceptPhasePreWork` + PhasePage banners |
| F14 — manual BOM allowed but must reconcile to Cosmetri before Gate 7; Gates 10/11 use Cosmetri | ✅ done | `BomLine.reconciled` + `bomReconciled` check in `gateReadiness.ts` + `BomCosting.tsx` |

Deferred until the production phase (not answerable by config):
- Replace the Power Apps placeholder URL on the Integrations page when the real "Create new raw material" app link is available (stored in the settings, no code change needed).
- Real authentication/users (prerequisite for F6) and the backend per `docs/APP_PLAN.md` section 7.

### Config-driven pages, not hardcoded per-phase UI

Every phase sheet in the source Excel shares the same layout, so the app builds each phase page from **config + ~6 shared components** rather than one bespoke component per phase:

- `packages/shared/src/config/phases.ts` — `PHASE_CONFIGS` (keyed by phase 1-4): checklist sections, requirement-table rows, and key-gate-checks per phase, with option lists transcribed verbatim from the Excel.
- `packages/shared/src/config/registers.ts` — `REGISTER_CONFIGS`: ~30+ "evidence register" sheets (Supplier_RM_Evidence, Prohibited_Ingredients, PIF_Checklist_ASEAN, Formula_Change_Control, ...), each declared as a `RegisterConfig` (columns + `mode: 'register' | 'fixed'` + optional `fixedRows`). `mode: 'fixed'` = predefined reference rows the user annotates (status/evidence/notes editable, everything else `editable: false`); `mode: 'register'` = free-form rows the user adds/removes. Sidebar/command-palette navigation groups these (plus a few dedicated pages) by **responsibility only** ("WORKBOOK BY RESPONSIBILITY" in the sidebar) — the `DEPARTMENTS` array + `getNavGroups()`/`getNavGroup()`/`findNavGroupForRegister()` (the array/type names say "department" for historical reasons, but the axis is who's responsible/reviews, not formal HR org structure — e.g. "Quality & GMP" and "Quality" are separate responsibility groups). There used to be a second "by type" grouping (`REGISTER_CATEGORIES`) behind a sidebar toggle labeled "By responsibility" / "By type", removed 2026-07-17 in favor of a single authoritative axis — the removal briefly regressed the sidebar header to the literal string "WORKBOOK BY DEPARTMENT" (inconsistent with the "responsibility" wording the toggle itself used), fixed same-day.
- `packages/shared/src/config/evidence.ts` — `EVIDENCE_AREAS` for the Evidence Summary status board.
- `apps/web/src/pages/PhasePage.tsx` renders one phase entirely from `PHASE_CONFIGS[phase]` using the shared components below.
- `apps/web/src/pages/RegisterHubPage.tsx` renders a register category entirely from the responsibility nav groups (`getNavGroup`) + `DynamicTable`.

**When adding a new phase section or evidence register, add/edit a config entry — do not hand-write a new page/table component** unless the shape genuinely doesn't fit the existing 6 shared blocks.

**Any user-visible list/group order in this config (sidebar groups, register cards, dropdown options) should be verified against the actual source workbook's sheet order, not just against other code.** Two ordering bugs shipped and went unnoticed for ~3 days in 2026-07-17 because they type-checked fine and lint/build can't catch an order mistake — they only surfaced when the user cross-checked the rendered sidebar against the real `.xlsx`. To check the sheet order without opening Excel: the file is a zip — extract `xl/workbook.xml` (e.g. `Expand-Archive` after copying to a `.zip` extension) and read the `<sheet name="...">` elements in document order. Note this only applies to **order** — deliberate business remaps the user has already confirmed (e.g. R&I evidence filed under the "Quality" group, no separate R&I department) are NOT required to mirror the workbook 1:1.

The 6 reusable blocks (`apps/web/src/components/`): `ProjectIdentificationCard`, `GateFlowTable`, `ChecklistSection`, `RequirementTable`, `GateChecksTable`, `EightAnglesTable` + `SignOffBlock`. `DynamicTable` is the generic renderer for `RegisterConfig`-driven register tables.

### Editable tables: local draft + explicit Save, never per-keystroke store writes

Every inline-editable table (all 6 reusable blocks above, `DynamicTable`, `NextActionsCard`, `StudyApprovalCard`, `MarketTrackingCard`, the BOM/Packaging/Costing sections of `BomCosting.tsx`, `EvidenceSummary`, the editable columns of `PostMarketCapa`/`ChangeControl`, and the Graph settings on `IntegrationsPage`) uses `apps/web/src/hooks/useDraft.ts` + `apps/web/src/components/SaveBar.tsx` (decided 2026-07-17 — see the `draft-save-pattern` memory). Typing/selecting only updates local component state (instant, no lag); a "Save"/"Discard" bar appears while that draft differs from the committed store data, and Save commits everything in one write. This exists because a per-keystroke store write re-runs `JSON.stringify` + `localStorage.setItem` on the WHOLE persisted store (via `persist` middleware), which visibly lags as project data grows.

**When adding a new editable table, use this pattern — don't wire a new input straight to a per-row store setter.** The store only exposes **bulk** setters for table-shaped data (`setChecklistSection`, `setRequirementSection`, `setGateChecksBulk`, `setAnglesBulk`, `setSignOffsBulk`, `setPackagingBomBulk`, `setRegisterRowsBulk`, `setEvidenceItemsBulk`, `setCapaBulk`, `setChangesBulk`, `setNextActionsBulk`, `setMarketTracksBulk`, `setStudyApprovalsBulk`, plus `setBom`/`setCosting`) — the old per-row setters (`setChecklistItem`, `setRegisterRow`, `setBomLine`, ...) were deleted once nothing called them. Row add/remove also happens in the local draft (not by calling the store directly), so structural edits save atomically with the rest of the table. Modal-gated "Add new record" flows (CAPA, Change Control, Cosmetri import, formula version) are unaffected — they already commit only on an explicit submit button.

**Non-obvious pitfall:** `useDraft` compares its `committed` argument by **value** (JSON), not reference — many callers derive it inline (`.filter()`, `.map()`, `?? []`), which allocates a new array every render even when unchanged; a reference-based comparison resyncs (`setState`) on every render, which is an **infinite update loop** since resyncing triggers a re-render that re-derives yet another new reference (crashed with "Maximum update depth exceeded" the first time this shipped). Fixed via React's "adjust state during render" pattern rather than a `useEffect`.

### State & data

- **Zustand store** (`apps/web/src/store/useAppStore.ts`), persisted to `localStorage` (key `mbc360-demo-store`, versioned). All project mutations go through store actions (`setGate`, `setChecklistSection`, `setRegisterRowsBulk`, `addNextAction`, `setMarketTracksBulk`, `createFormulaVersion`, etc.) that call an internal `updateProject(id, updater)` — never mutate `ProjectData` in place from a component. Some actions also **enforce rules** (Gap blocks Proceed; launch approval requires PIF Approved; independent reviewer ≠ author department) — keep UI guards and store guards in sync.
- The persist `migrate` function **re-seeds instead of migrating** on schema-breaking version bumps (see the v1→v3 comments) — because this is a demo, old localStorage data is disposable; bump `version` and update `migrate`'s comment when `ProjectData`'s shape changes incompatibly.
- `apps/web/src/store/factory.ts` builds an empty `ProjectData` (and empty register rows) purely from the config files — so config is the single source of truth for what a brand-new project's forms look like.
- `apps/web/src/data/seed.ts` provides the demo seed projects/changes.
- `packages/shared/src/types/index.ts` is the canonical data model (mirrors `docs/APP_PLAN.md` section 3, intended to translate ~1:1 to a future backend schema per that doc's section 7).

### Routing / shell

`apps/web/src/App.tsx` — `HashRouter` with a persistent, **sticky** header/sidebar (`Shell`/`SideMenu`) that tracks the "active project" across global pages, plus per-project workspace nav (phase pages, BOM & Costing, Formulation Safety, Evidence Summary, Panel Feedback, Post-Market/CAPA, and register categories). Routes are all under `/projects/:projectId/...` except `/`, `/projects`, `/integrations`, `/change-control`, and `/admin/users` (only linked in the sidebar when the real signed-in session holds the admin role — see M2 below). There used to be a standalone global `/system-guide` page; it duplicated the `dept-system` register group's front-matter content, so on 2026-07-17 it was merged into that group as a "Guide" tab in `RegisterHubPage.tsx` (renamed "System Guide & Reference" — see `packages/shared/src/config/registers.ts`), reachable only from within a project's workspace nav like the rest of the register categories.
